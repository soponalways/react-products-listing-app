/* eslint-disable react/prop-types */
import React from 'react';

const Products = (props)=> {
    return (
        <div>
            <p>{props.message}</p>
        </div>
    )
}
export default Products;

